{
  "name": "vr-gun",
  "version": "0.1.0",
  "description": "Gun model stripped from A-Blast",
  "scripts": {
    "build": "cross-env NODE_ENV=production webpack",
    "start": "webpack-dev-server --host 0.0.0.0 --progress --colors --hot -d --inline",
    "lint": "semistandard -v | snazzy"
  },
  "semistandard": {
    "ignore": [
      "**/vendor/**"
    ]
  },
  "devDependencies": {
    "cross-env": "^3.0.0",
    "semistandard": "^9.0.0",
    "snazzy": "^4.0.1",
    "webpack": "^1.15.0",
    "webpack-dev-server": "^1.16.5"
  }
}
